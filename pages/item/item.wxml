<!--miniprogram/pages/item/item.wxml-->
<view class="total">
  <view class="adress">
    <image src="/images/tabBar/adress.png"></image>
    {{adress}}
  </view>
  <!-- <swiper class="swiper" indicator-dots="true" autoplay="true" interval="5000" duration="1000">  
        <block wx:for="{{movies}}" wx:for-index="index">  
          <swiper-item>  
            <image src="{{item.url}}" class="slide-image" mode="aspectFill"/>  
          </swiper-item>  
        </block>  
  </swiper>  -->
  <view class="movies">
    <image src="{{movies}}"></image>
  </view>
  <view class="item_name">
    物品名称：{{objectName}}
  </view>
  <view class="item_information">
    <view>所有者姓名：{{name}}</view>
    <view>QQ：{{phoneNumber}}</view>
    <view>服务类型：{{service}}</view>
  </view>
  <view class="explain">
    <view>详细信息：{{detail}}</view>
  </view>
</view>
